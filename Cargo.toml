[package]
name = "brad"
version = "0.1.0"
edition = "2021"

[features]
default = ["llvm"]
llvm = ["llvm-sys"]

[dependencies]
cfg-if = "1.0.0"
colored = "3.0"
clap = { version = "4.5.31", features = ["derive"] }
llvm-sys = { version = "191.0.0", optional = true }
rand = "0.9.0"