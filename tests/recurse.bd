type error<'a> = 'a

type io::error = error<str>
type fmt::error = error<str>

fn main {
    let e: fmt::error = foo
}

fn foo {
    let e = io::error (error "error")

    match e
    | error as e => e
}

