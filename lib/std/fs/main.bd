import std::io
import std::error

extern type file

#[link = "brad_file_open"]
extern fn file::open (path: str) (mode: str) -> file | io::error::file-not-found

#[link = "brad_file_write"]
extern fn file::write (file: file) (data: str) -> none | io::error::file-not-open

#[link = "brad_file_read"]
extern fn file::read (file: file) -> str | io::error::end-of-file

#[link = "brad_file_flush"]
extern fn file::flush (file: file) -> none

#[link = "brad_file_close"]
extern fn file::close (file: file) -> none
